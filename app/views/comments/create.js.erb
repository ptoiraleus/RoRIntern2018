<% if @comment.persisted? %>
$('.comments').append('<%= escape_javascript render 'comment', comment: @comment %>'); //escape_javascript można skrócić samym j
<% else %>
	alert('<%= j @comment.errors.full_messages.join(', ') %>');
<% end %>

$('.new-comment').find('input[type="text"]').val('');
